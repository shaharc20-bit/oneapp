apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod-shahar
  labels:
    app: nginx
    exam: q1
spec:
  containers:
    - name: nginx
      image: nginx:alpine
      ports:
        - containerPort: 80
