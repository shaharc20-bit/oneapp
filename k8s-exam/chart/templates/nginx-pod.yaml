apiVersion: v1
kind: Pod
metadata:
  name: {{ .Values.nginxPod.name }}
  labels:
    app: nginx
    {{- include "k8s-exam.labels" . | nindent 4 }}
spec:
  containers:
    - name: nginx
      image: {{ .Values.nginxPod.image }}
      ports:
        - containerPort: 80
